# ML Face Service - Environment Configuration
# Copy this file to .env and modify as needed

# Server Configuration
SERVER_PORT=8080

# Redis Configuration
REDIS_URL=redis://127.0.0.1:6379

# Optional: Set Redis URL with authentication for production
# REDIS_URL=redis://:your_secure_password@your-redis-host:6379
# Or with username: REDIS_URL=redis://username:password@host:port

# Development Note:
# For local development, the startup script will automatically detect
# a running Redis instance on localhost. You don't need to set these
# environment variables unless you want to override the defaults.
